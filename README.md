## Getting Started

First, run the Flask server:

```bash
cd Components/api
python yahooFinanceFetch-1D.py
```

Then run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```
